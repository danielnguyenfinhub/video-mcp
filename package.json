{
  "name": "video-mcp",
  "version": "1.0.0",
  "description": "Local video editing MCP server for Claude Desktop — powered by FFmpeg, Whisper, yt-dlp",
  "type": "module",
  "scripts": {
    "build": "npx esbuild src/index.ts --bundle --platform=node --format=cjs --outfile=dist/index.js",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.21.0",
    "typescript": "^5.5.0"
  }
}
